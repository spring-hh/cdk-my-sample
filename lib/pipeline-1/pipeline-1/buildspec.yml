version: 0.2

env:
  variables:
    NODE_ENV: "production"

phases:
  install:
    commands:
      - node -v
      - nvm -v
      - nvm install 18.12.1
      - nvm use 18.12.1
      - node -v
      - npm i
  build:
    commands:
      - npm run build
